// Auth Controller
// Handles: register, login, logout
// TODO: Fill in Sprint 1

const register = async (req, res) => {
  res.json({ message: 'Register controller — TODO Sprint 1' });
};

const login = async (req, res) => {
  res.json({ message: 'Login controller — TODO Sprint 1' });
};

const logout = async (req, res) => {
  res.json({ message: 'Logout controller — TODO Sprint 1' });
};

module.exports = { register, login, logout };